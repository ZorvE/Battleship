import javafx.scene.image.Image;
import javafx.scene.paint.ImagePattern;
import javafx.scene.shape.Circle;

public class NormalShell extends Shell {

    public NormalShell(double x, double y) {
        super(x, y, 10);
    }
}
